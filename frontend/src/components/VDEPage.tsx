import { useState } from "react";
const API = "http://127.0.0.1:8000";
export function VDEPage(){ const [topic,setTopic]=useState("0100-600"); const [text,setText]=useState(""); async function load(){ const res=await fetch(`${API}/work/vde?topic=${encodeURIComponent(topic)}`); const data=await res.json(); setText(data.text || JSON.stringify(data.snippets || data,null,2)); } return <div className="aufg-root"><div className="calc-title">VDE TEXTBAUSTEINE</div><div className="set-hint" style={{marginTop:-8}}>Kurze Arbeitsnotizen. Keine Rechtsberatung und keine vollständige Normauslegung.</div><div className="aufg-input-row"><input className="sap-input aufg-input" value={topic} onChange={e=>setTopic(e.target.value)} placeholder="dguv, 0100-600, 0105-100, angebot"/><button className="termin-parse-btn" onClick={load}>LADEN</button></div>{text&&<div className="sap-preview"><div className="sap-preview-header"><span className="sap-preview-label">BAUSTEIN</span><button className="sap-copy-btn" onClick={()=>navigator.clipboard?.writeText(text)}>KOPIEREN</button></div><pre className="sap-preview-text">{text}</pre></div>}</div> }
